#include <stdio.h>
#include "protocol.h"

int server_run(void) {
    printf("[server] server_run(): not implemented yet\n");
    (void)TLV_TYPE_LIST_REQUEST; // to avoid unused variable warning
    return 0;
}